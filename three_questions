# check_Palindrome

def checkPalindrome(inputString):
	a=''
	for i in range(1,len(inputString)+1):
			a+=inputString[-i]
	if inputString == a:
		return True
	else:
		return False 
    
print (checkPalindrome('aabaa'))
